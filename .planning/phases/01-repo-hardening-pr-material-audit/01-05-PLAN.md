---
phase: 01-repo-hardening-pr-material-audit
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [".planning/gsd-pr/PR_BODY.md", ".planning/gsd-pr/PR_TITLE.txt", ".planning/gsd-pr/STEP1_DISCUSSION_POST.md", ".planning/gsd-pr/README_DIFF.md"]
autonomous: true
requirements: ["AUDIT-01", "AUDIT-02", "AUDIT-03", "AUDIT-04"]
user_setup: []
must_haves:
  truths:
    - "PR_BODY.md opens with `## Enhancement PR` as the first H2"
    - "PR_BODY.md contains all 8 required H2 headings in the correct order"
    - "PR_TITLE.txt reads exactly `docs(NNNN): list gsd-book-skill in Community table`"
    - "STEP1_DISCUSSION_POST.md and PR_BODY.md frame the PR as zero-maintenance-burden, not category expansion"
    - "README_DIFF.md applies cleanly against current upstream `gsd-build/get-shit-done@main`"
  artifacts:
    - ".planning/gsd-pr/PR_BODY.md"
    - ".planning/gsd-pr/PR_TITLE.txt"
    - ".planning/gsd-pr/STEP1_DISCUSSION_POST.md"
    - ".planning/gsd-pr/README_DIFF.md"
  key_links:
    - "Heading order matches what scripts/pr-template-policy.cjs upstream expects"
---

<objective>
Audit and fix the 4 drafted PR-prep documents in `.planning/gsd-pr/` so they pass upstream's procedural gates on first submission.

Purpose: ~70% of community PRs to gsd-build/get-shit-done die on procedural violations. The PR_BODY.md missing `## Enhancement PR` heading is the single highest-leverage catch from research. Fix all four documents now so Phase 3 (open PR) succeeds on first try.
Output: 4 updated files in `.planning/gsd-pr/`.
</objective>

<context>
@.planning/phases/01-repo-hardening-pr-material-audit/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/gsd-pr/PR_BODY.md
@.planning/gsd-pr/PR_TITLE.txt
@.planning/gsd-pr/STEP1_DISCUSSION_POST.md
@.planning/gsd-pr/README_DIFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT-01 — PR_BODY.md heading audit and reorder</name>
  <files>.planning/gsd-pr/PR_BODY.md</files>
  <read_first>.planning/gsd-pr/PR_BODY.md, .planning/research/PITFALLS.md</read_first>
  <action>
1. Read `.planning/gsd-pr/PR_BODY.md` and list its current H2 (`^## `) headings in order.
2. Compare against the required order (from PITFALLS.md, upstream `scripts/pr-template-policy.cjs`):
   ```
   ## Enhancement PR
   ## Linked Issue
   ## What this enhancement improves
   ## Before / After
   ## How it was implemented
   ## Testing
   ## Scope confirmation
   ## Checklist
   ```
3. Restructure PR_BODY.md so all 8 headings appear in this exact order. Preserve all existing content under reasonable section boundaries — if existing content was under a heading not in the required list, move it under the closest required heading (e.g., content under `## Description` becomes content under `## What this enhancement improves`). If a required heading has no existing content, add a one-line placeholder appropriate to the heading (e.g., under `## Testing`, write "Documentation-only change; no automated tests are added or modified.").
4. The first H2 in the file MUST be `## Enhancement PR`. Add a short paragraph under it summarizing the PR (e.g., "Add a single row to the existing `## Community` table in README.md listing `kdp-book-launch` (a Claude Code skill for KDP book launches) as a community-authored project built on GSD. Closes #NNNN."). Use `NNNN` as a literal placeholder for the issue number.
  </action>
  <verify>head -1 .planning/gsd-pr/PR_BODY.md (should not be required headings check) — use this instead:
    `awk '/^## /' .planning/gsd-pr/PR_BODY.md` should output the 8 required headings in exact order.</verify>
  <acceptance_criteria>
    - `grep -c "^## " .planning/gsd-pr/PR_BODY.md` returns at least 8
    - `awk '/^## /{print}' .planning/gsd-pr/PR_BODY.md | head -1` outputs `## Enhancement PR`
    - All 8 required H2 headings present and in the canonical order
    - `Closes #NNNN` present somewhere in the body (issue number placeholder)
    - No default-template scolding markers present: `grep -F "'Wrong template'"`, `grep -F "'Every PR must use a typed template'"`, `grep -F "'Select the template that matches your PR'"` all return zero hits
  </acceptance_criteria>
  <done>PR_BODY.md passes pr-template-policy.cjs heading check.</done>
</task>

<task type="auto">
  <name>Task 2: AUDIT-02 — PR_TITLE.txt verification</name>
  <files>.planning/gsd-pr/PR_TITLE.txt</files>
  <read_first>.planning/gsd-pr/PR_TITLE.txt</read_first>
  <action>Read PR_TITLE.txt. Confirm it reads exactly (single line, no trailing newline issues):
```
docs(NNNN): list gsd-book-skill in Community table
```
If current content differs, overwrite to this exact string. `NNNN` must be the literal placeholder — not a real number, not `{issue#}`, not anything else. The substitution to a real issue number happens in Phase 3.</action>
  <verify>cat .planning/gsd-pr/PR_TITLE.txt | head -1</verify>
  <acceptance_criteria>
    - PR_TITLE.txt content matches exactly: `docs(NNNN): list gsd-book-skill in Community table`
    - File is single-line (no extra trailing lines)
    - `NNNN` placeholder preserved (not substituted)
  </acceptance_criteria>
  <done>PR_TITLE.txt locked to canonical form.</done>
</task>

<task type="auto">
  <name>Task 3: AUDIT-03 — Framing audit on STEP1_DISCUSSION_POST.md and PR_BODY.md</name>
  <files>.planning/gsd-pr/STEP1_DISCUSSION_POST.md, .planning/gsd-pr/PR_BODY.md</files>
  <read_first>.planning/gsd-pr/STEP1_DISCUSSION_POST.md, .planning/gsd-pr/PR_BODY.md, .planning/research/FEATURES.md</read_first>
  <action>
Scan both files for the following anti-patterns and rephrase. Document each change you make in your task summary.

Anti-patterns to flag:
1. Verbs that imply expansion of the Community section ("expand", "grow", "add to the section", "introduce a new"): rewrite to "add one row to the existing Community table" or similar minimal framing.
2. Phrases that read as marketing/promotional: "powerful", "complete solution", "the only skill that", "showcases", etc. — rewrite as factual neutral statements.
3. "Built on GSD" framing in a self-promotional tone — keep TÂCHES credit but pitch as factual (this skill uses GSD's command surface) not as a feature of this skill.
4. Anything that reads as feature pitch or category-expansion request rather than a one-row docs enhancement.

Replacement framings to use:
- Frame as "external project that uses GSD as documented; zero maintenance burden on TÂCHES"
- Frame as "first community-authored skill row" (factual: maintainer added gsd-opencode; this is the first community-authored row)
- Frame the PR diff explicitly: "1 row added to `## Community` table in README.md + 1 `.changeset/` fragment, no other files"

For each rewrite, preserve all factual content (links, TÂCHES credit, conformance audit references) — only the wording changes.</action>
  <verify>
    - `! grep -iE "expand the community|powerful|complete solution|the only skill" .planning/gsd-pr/STEP1_DISCUSSION_POST.md .planning/gsd-pr/PR_BODY.md`
    - `grep -iE "zero maintenance|external project|community-authored" .planning/gsd-pr/STEP1_DISCUSSION_POST.md` (at least one match)
  </verify>
  <acceptance_criteria>
    - No "expand the Community" or category-expansion phrasing remains
    - No "powerful" / "complete solution" / "the only" promotional adjectives
    - "Zero maintenance burden" or "external project" framing appears at least once in STEP1_DISCUSSION_POST.md
    - TÂCHES credit preserved
    - Conformance audit references preserved
  </acceptance_criteria>
  <done>Framing audit complete — both files free of expansion/marketing phrasing.</done>
</task>

<task type="auto">
  <name>Task 4: AUDIT-04 — README_DIFF.md upstream-main verification</name>
  <files>.planning/gsd-pr/README_DIFF.md</files>
  <read_first>.planning/gsd-pr/README_DIFF.md</read_first>
  <action>
1. Unset `GITHUB_TOKEN` for this task (use keyring auth): `unset GITHUB_TOKEN`
2. Fetch the current upstream README from `gsd-build/get-shit-done@main`:
   ```
   gh api repos/gsd-build/get-shit-done/contents/README.md --jq '.content' | base64 -d > /tmp/upstream-README.md
   ```
   (Or via curl: `curl -sL https://raw.githubusercontent.com/gsd-build/get-shit-done/main/README.md > /tmp/upstream-README.md`)
3. Find the `## Community` section in the current upstream README. Capture its current line numbers and the exact format of the existing rows (gsd-opencode and Discord). Compare against what `.planning/gsd-pr/README_DIFF.md` assumes.
4. If line numbers drift but the section structure is the same: update `README_DIFF.md` to reference the current line numbers; keep the same added row.
5. If the section structure changed (new column, reorganized, etc.): STOP and flag — that's a scope expansion outside this plan.
6. Confirm the added row is exactly one table row matching the existing table's column count.
7. Clean up `/tmp/upstream-README.md` at the end.
  </action>
  <verify>
    - `.planning/gsd-pr/README_DIFF.md` references the current `## Community` line numbers
    - Diff adds exactly 1 line in the table body
    - No structural changes to the table (same column count as upstream)
  </verify>
  <acceptance_criteria>
    - README_DIFF.md line numbers match current upstream HEAD
    - Single-row addition confirmed
    - Column count of added row matches upstream's current table
    - /tmp/upstream-README.md cleaned up
  </acceptance_criteria>
  <done>README_DIFF.md verified against current upstream main.</done>
</task>

</tasks>

<summary_target>
.planning/phases/01-repo-hardening-pr-material-audit/01-05-SUMMARY.md
</summary_target>
