---
phase: 01-repo-hardening-pr-material-audit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: ["examples/README.md", "examples/book_config.json.example", "examples/launch_config.json.example"]
autonomous: true
requirements: ["POLISH-03"]
user_setup: []
must_haves:
  truths:
    - "examples/ directory exists with a README, book_config.json.example, and launch_config.json.example"
    - "A reviewer can read examples/README.md and understand the contract for skill inputs"
  artifacts:
    - "/Users/shoemoney/Projects/gsd-book-skill/examples/README.md"
    - "/Users/shoemoney/Projects/gsd-book-skill/examples/book_config.json.example"
    - "/Users/shoemoney/Projects/gsd-book-skill/examples/launch_config.json.example"
  key_links:
    - "examples/README.md points back at skill/SKILL.md for canonical workflow"
    - "examples/*.example mirror the structure of skill/templates/*.template"
---

<objective>
Populate the previously-empty `examples/` directory with a README + two example configs that explain the input contract for the skill, without committing any actual manuscript text.

Purpose: An empty `examples/` directory is a red flag to a reviewer ("did they forget?"). A README-only `examples/` answers the question that empty raises: "how do I use this thing?"
Output: 3 files under `examples/`.
</objective>

<context>
@.planning/phases/01-repo-hardening-pr-material-audit/01-CONTEXT.md
@skill/SKILL.md
@skill/templates/book_config.json.template
@skill/templates/launch_config.json.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the canonical config templates</name>
  <files>(read-only)</files>
  <read_first>skill/templates/book_config.json.template, skill/templates/launch_config.json.template, skill/SKILL.md</read_first>
  <action>Read both `.template` files in `skill/templates/`. Note their structure: top-level keys, chapter array shape with `slug` join key (per ARCHITECTURE.md), any `"_comment"` strip-comment annotations. Note: the templates are the canonical source — examples should mirror them, not invent a new shape.</action>
  <verify>test -f skill/templates/book_config.json.template && test -f skill/templates/launch_config.json.template</verify>
  <acceptance_criteria>
    - Template structures are known
  </acceptance_criteria>
  <done>Template shapes captured.</done>
</task>

<task type="auto">
  <name>Task 2: Create examples/ directory</name>
  <files>examples/.gitkeep (transient — removed after Task 3 if other files exist)</files>
  <action>Run `mkdir -p examples`. The directory may already appear in `git ls-files` as a missing entry; that's fine — the next tasks populate it.</action>
  <verify>test -d examples</verify>
  <acceptance_criteria>
    - examples/ directory exists
  </acceptance_criteria>
  <done>Directory ready.</done>
</task>

<task type="auto">
  <name>Task 3: Write examples/README.md</name>
  <files>examples/README.md</files>
  <action>Create examples/README.md with content explaining the input contract. Keep it short — 30-60 lines. Required content:

- One-paragraph orientation: this directory documents what a real book launch project looks like — drop your own manuscript + configs and run the skill via `/gsd-new-project` (or directly via the scripts under `skill/scripts/`)
- Expected directory layout for a real launch (the user's repo, not this one):
  ```
  my-book/
    chapters/
      01-introduction.md
      02-chapter-name.md
      ...
    book_config.json
    launch_config.json
  ```
- Pointer to `examples/book_config.json.example` and `examples/launch_config.json.example` as starter configs
- Pointer to `../skill/SKILL.md` as the canonical workflow doc
- Explicit note: "We do NOT ship a sample manuscript. Use your own text."
- Brief mention of the 5-phase pipeline (Manuscript Lock → Chapter Illustrations → Cover Production → Build & Compile → KDP Launch Prep) and `[GATE]` checkpoints, with a one-line pointer to `skill/references/phase-checklist.md` for the detailed runbook.

Tone: factual, no marketing, no promotional language. This file will be read by a reviewer along with the rest of the repo.</action>
  <verify>test -f examples/README.md && grep -q "manuscript" examples/README.md && grep -q "skill/SKILL.md" examples/README.md</verify>
  <acceptance_criteria>
    - examples/README.md exists
    - References examples/book_config.json.example and examples/launch_config.json.example
    - References skill/SKILL.md
    - Includes the directory-layout block
    - Includes the "no sample manuscript" note
  </acceptance_criteria>
  <done>examples/README.md explains the input contract.</done>
</task>

<task type="auto">
  <name>Task 4: Write examples/book_config.json.example</name>
  <files>examples/book_config.json.example</files>
  <read_first>skill/templates/book_config.json.template</read_first>
  <action>Create examples/book_config.json.example by adapting `skill/templates/book_config.json.template` into a concrete-but-fictional example. Replace placeholder tokens with realistic-looking sample values. Use a generic placeholder title like "My Book Title" and author "Your Name". Include 2-3 sample chapters in the chapters array, each with `slug` matching a `chapters/NN-slug.md` filename pattern. Preserve the `"_comment"` key convention if the template uses it — those are stripped by `_config.py:_strip_comments`. Final file should be valid JSON (no trailing comments outside `_comment` keys).</action>
  <verify>test -f examples/book_config.json.example && python -c "import json; json.load(open('examples/book_config.json.example'))"</verify>
  <acceptance_criteria>
    - examples/book_config.json.example exists
    - Parses as valid JSON
    - Contains at least 2 entries in chapters array with `slug` keys
    - Contains placeholder title + author (not real PII)
  </acceptance_criteria>
  <done>Example book config ready.</done>
</task>

<task type="auto">
  <name>Task 5: Write examples/launch_config.json.example</name>
  <files>examples/launch_config.json.example</files>
  <read_first>skill/templates/launch_config.json.template</read_first>
  <action>Same as Task 4 but for the launch config — adapt `skill/templates/launch_config.json.template` into a runnable-looking example. Placeholder values for ISBN, KDP marketplace settings, image-gen model selection, etc. Preserve the structure from the template; only swap placeholders for concrete sample values. Final file must be valid JSON.</action>
  <verify>test -f examples/launch_config.json.example && python -c "import json; json.load(open('examples/launch_config.json.example'))"</verify>
  <acceptance_criteria>
    - examples/launch_config.json.example exists
    - Parses as valid JSON
    - Mirrors the structure of skill/templates/launch_config.json.template
  </acceptance_criteria>
  <done>Example launch config ready.</done>
</task>

</tasks>

<summary_target>
.planning/phases/01-repo-hardening-pr-material-audit/01-04-SUMMARY.md
</summary_target>
